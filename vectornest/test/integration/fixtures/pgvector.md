# pgvector

pgvector is a Postgres extension that adds a vector data type and vector similarity search.
It supports cosine distance, L2 distance, and inner product, and offers HNSW and IVFFlat
indexes for approximate nearest-neighbor search over embeddings produced by machine-learning
models.
